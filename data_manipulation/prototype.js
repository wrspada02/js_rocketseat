
    //Quando criamos um dado, ele ja e encapsulado por objetos que ja tem propriedades definidas, que podemos utilizar para manipular os dados. Segue exemplo abaixo;
    //Javascript e uma linguagem baseada em prototipo, ou seja, funcionalidades prontas para utilizarmos
    //Mesmo nao declarando um objeto como no caso abaixo, podemos acessar as funcoes dos objetos acima criado pelo js quando vamos acessando com ponto
    //Exemplo : name.length

    let name = `William`;
    console.log(name.length); // = 7;
